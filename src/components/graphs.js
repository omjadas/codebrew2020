import React from "react";

import {
  XYPlot,
  XAxis,
  YAxis,
  HorizontalGridLines,
  VerticalGridLines,
  LineSeries
} from "react-vis";

const MSEC_DAILY = 86400000;

export const Graphs = (props) => {

};
